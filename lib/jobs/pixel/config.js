const rootPrefix = '../../..',
  pixelConstants = require(rootPrefix + '/lib/globalConstant/pixel');

module.exports = {
  [pixelConstants.accountUpdateEntityType]: {
    mandatory: [],
    optional: []
  }
};
