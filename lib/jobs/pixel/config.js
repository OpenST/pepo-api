const rootPrefix = '../../..',
  pixelConstants = require(rootPrefix + '/lib/globalConstant/pixel');

module.exports = {
  [pixelConstants.userEntityType]: {
    mandatory: [],
    optional: []
  }
};
