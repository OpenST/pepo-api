const rootPrefix = '../..',
  util = require(rootPrefix + '/lib/util');

/**
 * Class for User Activity constants.
 *
 * @class
 */
class UserActivityConstants {
  /**
   * Constructor for User Activity constants.
   *
   * @constructor
   */
  constructor() {}
}

module.exports = new UserActivityConstants();
