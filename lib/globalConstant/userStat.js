const rootPrefix = '../..',
  util = require(rootPrefix + '/lib/util');

/**
 * Class for user stat constants.
 *
 * @class
 */
class UserStatConstants {
  /**
   * Constructor for user stat constants.
   *
   * @constructor
   */
  constructor() {}
}

module.exports = new UserStatConstants();
