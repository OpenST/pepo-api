const rootPrefix = '../..',
  util = require(rootPrefix + '/lib/util');

/**
 * Class for user contributed by constants.
 *
 * @class
 */
class UserContributedByConstants {
  /**
   * Constructor for user contributed by constants.
   *
   * @constructor
   */
  constructor() {}
}

module.exports = new UserContributedByConstants();
