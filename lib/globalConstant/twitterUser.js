const rootPrefix = '../..',
  util = require(rootPrefix + '/lib/util');

/**
 * Class for twitter user constants.
 *
 * @class
 */
class TwitterUserConstants {
  /**
   * Constructor for twitter user constants.
   *
   * @constructor
   */
  constructor() {}
}

module.exports = new TwitterUserConstants();
