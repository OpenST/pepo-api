const rootPrefix = '../..',
  util = require(rootPrefix + '/lib/util');

/**
 * Class for video detail constants.
 *
 * @class
 */
class VideoDetailConstants {
  /**
   * Constructor for Video Details constants.
   *
   * @constructor
   */
  constructor() {}
}

module.exports = new VideoDetailConstants();
